<h1>Tweet Now! 2: Multi-User</h1>

<%
  # In our real-world application, this is not what we want. Instead, we should
  # be checking to see if there is currently a user logged-in, and if there is,
  # we should show her a form that will allow her to send a tweet. If there is no
  # user logged-in, we should show the sign-in button.
%>
<% if logged_in? %>
  <div>
    <p>@<%=current_user.username%></p>
      <form id="tweet_form" class='pure-form pure-form-stacked' action='/tweet/create' method='POST'>
        <label>Tweet Something<textarea cols='50' rows='10' name='tweet[text]' placeholder='Your Tweet'></textarea></label>
        <input class='pure-button pure-button-primary' type='submit' value="Submit">
      </form>
    <p>
      <button class="pure-button pure-button-secondary"><a href="/sign_out">Sign Out</a></button>
    </p>
  </div>
<% else %>
  <button class="pure-button pure-button-primary"><a href="/sign_in">Sign In with Twitter</a></button>
<% end %>

